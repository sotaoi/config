{
  "name": "@sotaoi/config",
  "private": false,
  "version": "0.9.6",
  "description": "",
  "deploymentScheme": {
    "init": "",
    "copy": {
      "skip": false,
      "src": "./",
      "exclude": [
        "./node_modules"
      ]
    },
    "catch": "",
    "conclude": ""
  },
  "scripts": {
    "bootstrap": "npm install --legacy-peer-deps --force --no-audit --no-fund && node ./scripts/build"
  },
  "devDependencies": {
    "@sotaoi/config": "file:./",
    "@types/node": "^14",
    "typescript": "4.1.5"
  }
}
