{
  "name": "@sotaoi/config",
  "private": false,
  "version": "0.9.7",
  "description": "",
  "deploymentScheme": {
    "init": "",
    "copy": {
      "skip": false,
      "src": "./",
      "exclude": [
        "./node_modules"
      ]
    },
    "catch": "",
    "conclude": ""
  },
  "scripts": {
    "bootstrap": "npm install --no-optional --legacy-peer-deps --force --no-audit --no-fund  --loglevel error && node ./scripts/build"
  },
  "devDependencies": {
    "@sotaoi/config": "file:./",
    "@types/node": "^14",
    "typescript": "4.1.5"
  }
}
